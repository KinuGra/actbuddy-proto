import { SettingsMain } from '@/features/settings'

export const metadata = {
  title: '設定 - ActBuddy',
}

export default function SettingsPage() {
  return (
    <main>
      <SettingsMain />
    </main>
  )
}
